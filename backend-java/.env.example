# Environment Variables for Service Dashboard Backend

# Vault AppRole Credentials
VAULT_ROLE_ID=your-vault-role-id-here
VAULT_SECRET_ID=your-vault-secret-id-here

# Database Configuration (managed by Vault)
# These are automatically fetched from Vault at runtime:
# - db_url
# - db_username
# - db_password
